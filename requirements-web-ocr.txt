# مكتبات قراءة الجوازات (OCR) — اختيارية للويب.
# ثبّتها فقط إن أردت تفعيل «📷 قراءة جواز» على الاستضافة، بعد requirements-web.txt:
#     pip install -r requirements-web-ocr.txt
# تتطلّب أيضاً محرّك tesseract مثبّتاً على الخادم مع حزمة اللغة العربية (ara).
# على PythonAnywhere محرّك tesseract واللغة العربية مثبّتان مسبقاً على النظام.
# إن لم تُثبَّت هذه المكتبات، يبقى البرنامج يعمل ويظهر تنبيه لطيف في صفحة القراءة.
opencv-python-headless>=4.8
numpy>=1.24
pytesseract>=0.3.10
